#!/usr/bin/env bash
#
# Infrastructure Analyzer - Unified CLI wrapper
# This is a convenience wrapper for the Python CLI
#

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Python CLI
exec python3 "${SCRIPT_DIR}/cli.py" "$@"
